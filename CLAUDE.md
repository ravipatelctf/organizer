# Organizer

@AGENTS.md

## Claude Code specifics

- Slash commands live in `.claude/commands/`.
- Permission allowlist is in `.claude/settings.json`.
- Architecture reference: `docs/architecture.md`.
