# apps/web/.env — copy this file there.
# Empty locally: the api runs on its own origin (http://localhost:8000) and lib/env.ts talks to
# it directly. Set to "/api" on Vercel, where apps/web/vercel.ts rewrites /api/* to the api
# project so the two are same-origin.
NEXT_PUBLIC_API_BASE_PATH=
